import React from 'react';

// * Components
import { FooterContainer, TitleFooter } from './footer.style';

// TODO ask client how he wanna see the footer

export const Footer = () => {
  return (
    <FooterContainer>
      <TitleFooter>Footer</TitleFooter>
    </FooterContainer>
  );
};

export default Footer;
